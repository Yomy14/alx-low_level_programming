#include <stdio.h>
/**
 * main - Entry point
 * Description: Prints alphabet in lowercase and in reverse
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha;

	for (alpha = 'z'; alpha >= 'a'; alpha--)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
